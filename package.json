{
  "name": "codex-monitor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Dependency-light Codex execution monitor with a web dashboard.",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/lib/monitor.js && node --check src/lib/session-ingest.js && node --check hooks/send_event.js"
  },
  "dependencies": {
    "chart.js": "^4.5.1"
  }
}
